# Currency-Converter-web-project-
Presentation:https://www.canva.com/design/DAG5xqvzyNE/7kwdSlZencp8PRaamSKzTw/edit?utm_content=DAG5xqvzyNE&utm_campaign=designshare&utm_medium=link2&utm_source=sharebutton)
Figma:https://www.figma.com/design/RScq5hqHl8vhJISl7ntqb9/currency-converter?node-id=0-1&p=f&t=LZB61042AtX4hLvo-0





**Currency Converter** — это веб-приложение для мгновенной конвертации валют с использованием реальных рыночных данных и сохранением истории операций.

## 🚀 Функционал

  * **Актуальные курсы:** Конвертация валют в реальном времени.
  * **История операций:** Автоматическое сохранение последних 5 вычислений.
  * **Сохранение данных:** История остается доступной даже после перезагрузки страницы (используется LocalStorage).
  * **Надежность:** Защита от зависаний при плохом интернете (Timeout 9 сек).
  * **Умная фильтрация:** Исключение дубликатов в истории поиска.

## 🛠️ Технологии

  * **HTML** – структура приложения.
  * **CSS** – стилизация и адаптивность.
  * **JavaScript** – основная логика.
      * `Async/Await` для асинхронных запросов.
      * `AbortController` для управления тайм-аутами.
      * `LocalStorage` для хранения истории.
  * **FreeCurrencyAPI** – источник валютных курсов.

## 🔧 Настройка и запуск

1.  **Клонировать проект:**

    ```bash
    git clone https://github.com/your-username/currency-converter.git
    ```

2.  **Перейти в папку проекта:**

    ```bash
    cd currency-converter
    ```

3.  **Запустить:**
    Просто откройте файл `index.html` в вашем браузере.

4.  **Настройка API:**
    Проект использует **FreeCurrencyAPI**.
    В файле `script.js` найдите переменную `API_KEY` и при необходимости замените её на свой ключ:

    ```javascript
    const API_KEY = 'ВАШ_API_KEY';
    ```

## 🔑 Работа с API

Приложение использует **FreeCurrencyAPI** (`https://api.freecurrencyapi.com/v1/latest`).

  * **Запрос:** Отправляется `fetch` запрос с указанием базовой валюты и целевых валют.
  * **Безопасность:** Реализован механизм `fetchWithTimeout`, который прерывает запрос, если сервер не отвечает более 9 секунд.
  * **Формат ответа:**
    ```json
    {
      "data": {
        "EUR": 0.92,
        "RUB": 90.5
      }
    }
    
    Приложение извлекает нужный коэффициент и производит расчет.
```
📂 Структура проекта
│── index.html      # Основная разметка страницы
│── styles.css      # Стили
│── script.js       # Логика приложения (загрузка цитат)
└── README.md       # Описание проекта

